# Navigate to your project directory (if not already there)
cd /path/to/AscendOS

# Initialize Git
git init

# Add all files to the repository
git add .

# Commit the changes with a message
git commit -m "Initial commit - AscendOS"

# Link your local project to GitHub (ryanezell16)
git remote add origin https://github.com/ryanezell16/AscendOS.git

# Push the project to GitHub
git branch -M main
git push -u origin main
